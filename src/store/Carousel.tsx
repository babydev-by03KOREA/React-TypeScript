import React from 'react';
import { Swiper, SwiperSlide } from "swiper/react";

const Carousel = () => {
    return (
        <React.Fragment>

        </React.Fragment>
    );
};

export default Carousel;
